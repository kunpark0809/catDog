<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="park">
	<insert id="insertPark" parameterType="com.catDog.park.Park">
		INSERT INTO recommend(recommendNum, sortNum, num, placeName, content, tel, hitCount, addr, lat, lon, created)
		VALUES (recommend_seq.NEXTVAL, #{sortNum}, #{num}, #{placeName}, #{content}, #{tel}, #{hitCount}, #{addr}, #{lat}, #{lon}, #{created})
	</insert>
	
</mapper>